{
  "name": "tailwind-boilerplate",
  "source": "src/index.html",
  "browserslist": "> 0.5%, last 2 versions, not dead",
  "scripts": {
    "dev": "parcel src/index.html",
    "build": "NODE_ENV=production rm -rf ./dist && parcel build --public-url ./ src/index.html",
    "postbuild": "du -sh -I'*.map' dist"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4.0.6",
    "parcel": "latest",
    "posthtml-include": "^2.0.1",
    "svgo": "^3.3.2"
  },
  "dependencies": {
    "flowbite": "^3.1.2",
    "tailwindcss": "^4.0.6"
  }
}
